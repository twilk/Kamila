export const COUNTER_CONFIG = {
    STORAGE_KEY: 'counter_settings',
    AUTO_REFRESH: true,
    REFRESH_INTERVAL: 60000, // 1 minute
    ANIMATION: {
        enabled: true,
        duration: 300 // ms
    }
}; 