export const UPDATE_CONFIG = {
    CHECK_INTERVAL: 60 * 60 * 1000, // 1 hour
    STORAGE_KEY: 'update_settings',
    AUTO_UPDATE: true,
    NOTIFICATION: {
        enabled: true,
        timeout: 0 // 0 = no timeout
    }
}; 